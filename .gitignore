node_modules/
dist/
build/
.pnpm-store/
coverage/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Local editor and operating-system files
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local AI workspace guidance (not required by the app or deployment)
/AGENTS.md
/CLAUDE.md

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
